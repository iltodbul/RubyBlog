<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= article.title %>
    </li>
    <li>
      <% time = article.created_at.localtime %>
      Article created at:
      <%= time.strftime("%d/%m/%Y - %H:%M:%S") %>
    </li>
  <% end %>
</ul>
